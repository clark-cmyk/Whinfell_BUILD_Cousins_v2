# Global Track Data Storage

Reserved for **Global track** Parquet datasets (Whinfell Score, transmission state, WTM exports).

| Version | Path |
|---------|------|
| v1 stub | `v1/global_observations.parquet` (0 rows; see `v1/README.md`) |

The **China Policy track** writes exclusively to `data/china_policy/`. No China Policy ingestion script reads or modifies files in this directory.